//  Ask two integers from user, and store them in variables A & B (using their pointers). 
//Add both the variables and store it in a varible C (using their pointers) and also print the answer C (using its pointer).
