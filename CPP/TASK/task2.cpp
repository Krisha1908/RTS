 #include<iostream>
#include<vector>
#include<string>
#include<list>

using namespace std;

    int Sum(vector<int>num,vector<char>ch,vector<string>input)
    {
        
    }

int main()
{
    string name;
    vector<int>num;
    vector<char>ch;
    vector<string>input;
    cout<<"Enter the Input : ";
    cin>>name;
    input.push_back(name);
    Sum(num,ch,input);
}
